1.路由传递参数（对象写法）path是否可以结合params参数一起使用？
  答：路由跳转传参的时候，对象的写法可以是name，path形式，但是需要注意的是，path这种写法不能与params参数一起使用
  
2.如何制定params参数可传可不传？
  答：如果路由要求传递params参数，但是你就不传递params参数，发现一件事，url会有问题
      在配置路由的时候，在占位的后面加一个问号，代表params可传可不传
      
3.params参数可以传递也可以不传递，但是如果传递的是空串如何解决？
  答：使用undfined解决：params参数可以传递，不传递（空字符串）
  
4.路由组件能不能传递props数据？
  答：可以的。
      布尔值写法：props：true 只能传递params
      对象写法：props：{a:1,b:2}额外给路由传递一些props
      函数写法：（$rpute）{
        return {keyword:$route.params.keyword,k:$route.query.k}
      }
      
